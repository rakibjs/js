loops: keep running the code blocks unless the condition is false.

Two loops:  for, while (In general)
Six loops: for, for_in,  for_of,  while, do_while,  forEach (javScripts)

for loop:

for (initialize iterator; condition; final experession){
    // statements
}

obj = {
    'name': 'Mark',
    'age' : 25,
    'nationality': 'English',
    'job': 'Banker',
}

obj.name || obj['name']

array = [
    'Mark', 
    '25', 
    'English', 
    'Banker'
];

array[0]
