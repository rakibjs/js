let colors = ['red', 'blue', 'black'];
let colorsJoined = colors.join(' ');

let numbers = [1, 2, 3,  4];

// numbers.pop();
//numbers.push(5);

// splice ( index of operation start, numver of elemnts to remove/add, value to be added)
//numbers.splice(1, 1, 5);

// slice(index of operation start, end index.)
// numbers[0] = 1
//const slicedNumbers =  numbers.slice(1, 3);
// Array class

const colors_and_numbers = numbers.concat(colors);


//console.log( typeof colors_and_numbers[5]); 
//console.log(numbers);